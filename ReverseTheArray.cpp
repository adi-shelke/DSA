#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> arr = {1, 2, 3, 4, 5, 6};

    int first = m + 1;
    int last = arr.size() - 1;

    while (first < last)
    {
        swap(arr[first], arr[last])
            first++;
        last++;
    }
}